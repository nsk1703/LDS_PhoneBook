{% extends 'base.html.twig' %}

{% block title %} Editing member's{% endblock %}

{% block body %}
    <!-- Content Header (Page header) -->
    <section class="content-header">
        {{ include('layouts/header_content/header_edit-form_content.html.twig') }}
    </section>
    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
          <div class="row justify-content-center">
              <div class="col-md-6">
                <!-- general form elements disabled -->
                <div class="card card-primary">
                    <div class="card-header"  style="text-align: center">
                        <h3 class="card-title">Updating member's</h3>
                    </div>
                    <!-- /.card-header -->
                    <div class="card-body">
                        {{ include('members/__form.html.twig', {submitButtonText: 'Edit a member'}) }}
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
              </div>
              <!--/.col (right) -->
            </div>
            <!-- /.row -->
        </div><!-- /.container-fluid -->
    </section>
    <!-- /.content -->
{% endblock %}