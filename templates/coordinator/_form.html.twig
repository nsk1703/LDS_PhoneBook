{{ form_start(form, {'attr': {'id': 'allAddForm'}}) }}

    {{ form_widget(form) }}

<input class="btn btn-success btn-block" type="submit" id="submit"
       value="{{ submitButtonText|default('Ajouter un Coordinateur') }}" formnovalidate>
{{ form_end(form) }}
